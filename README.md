# 共享单车调度优化问题

## 题目说明

本项目围绕共享单车站点运行数据，完成以下 3 个问题：

### 问题1：描述性分析

基于 `附件2_每小时借还记录.csv` 与 `附件1_站点基础信息.csv`，分析 30 个站点在一周内的借还规律，识别：

1. 全天典型借出峰、归还峰及持续时间；
2. 借出量最高 / 最低站点及其空间分布特征；
3. 工作日与休息日借还模式的主要差异。

### 问题2：未来 1 天需求预测

建立对目标日期 24 小时逐站点借出量、归还量的预测模型，并在测试日上评估精度。

当前实现采用：

- 小时周期特征：`hour_sin`、`hour_cos`
- 日期类型特征：`is_weekday`
- 可提前获得的历史特征：前一日同小时借还量 `lag_24`
- 训练集均值基线：同站点同小时历史均值
- 空间特征：站点聚类分区 `zone`

最终预测值采用“基线 + 残差模型”的方式得到。

### 问题3：调度优化

基于问题2的预测结果，对工作日进行“无调度 / 有调度”仿真对比，输出：

1. 总费用；
2. 满桩 / 空桩发生次数；
3. 用户无法借车 / 还车次数；
4. 用户需求满足率；
5. 调度明细与库存变化。

当前题3实现采用离散时延调度：

- 调度在整点发车；
- 调度时间按 `t_ij = round(d_ij / v)` 取整；
- 源站发车当小时立即扣减库存；
- 目标站在 `t + t_ij` 时刻入库。

---

## 当前目录结构

```text
shared_bike_scheduling/
├─ README.md
├─ .gitignore
├─ main.py
├─ problem1_eda.py
├─ problem2_workday_predict.py
├─ problem3_config.py
├─ problem3_simulation.py
├─ problem3_solution.py
├─ model_train_predict.py
├─ data_preprocess.py
├─ 附件1_站点基础信息.csv
├─ 附件2_每小时借还记录.csv
├─ 附件3_调度成本参数.csv
├─ outputs/
│  ├─ problem1/
│  ├─ problem2/
│  └─ problem3/
└─ docs/
   └─ archive/
```

整理原则：

- 根目录只保留“核心脚本 + 原始数据 + README”；
- 所有运行产物统一进入 `outputs/`；
- 重复、过时、误导性的说明文件移入 `docs/archive/`；
- `__pycache__/` 不再保留。

---

## 运行方式

### 问题1

```bash
python problem1_eda.py
```

输出位置：`outputs/problem1/`

### 问题2

```bash
python main.py
```

输出位置：

- 控制台：测试日误差指标
- `outputs/problem2/figures/`：代表站点预测曲线

### 为问题3生成工作日预测

```bash
python problem2_workday_predict.py
```

输出位置：`outputs/problem2/predictions_2025-04-11.csv`

### 问题3

```bash
python problem3_solution.py
```

输出位置：`outputs/problem3/`

---

## 本次检查后确认的关键问题

### 已修正的问题

1. 问题2原实现存在明显数据泄漏。  
   原脚本先对全量数据做基线与滞后特征，再拆训练 / 测试，等于在预测目标日时使用了目标日本身的信息。现在改为：先按目标日期切分，再仅用训练集构造基线。

2. 问题2原实现错误使用了 `lag_1 / lag_2 / lag_3 / rolling`。  
   这些特征在“整天提前预测”场景下不可直接获得，会隐式使用目标日真实值。现在保留仅在预测开始前可知的 `lag_24` 及静态特征。

3. 问题2原 MAPE 指标失真严重。  
   原公式在真实值为 0 时会产生极大数值，现在改为仅在非零真实值上计算 MAPE。

4. 问题1输出文件全部堆在根目录。  
   现已统一输出到 `outputs/problem1/`，并去掉了工作日固定除以 5、休息日固定除以 2 的硬编码假设。

5. 问题3自动读取预测文件的逻辑不稳定。  
   原实现会直接读取根目录第一个匹配到的 `predictions_*.csv`。现在优先读取 `outputs/problem2/` 中排序后的最新预测文件。

6. 问题3运输成本公式不合理。  
   原实现把“公里成本 + 单车搬运成本”整体再乘车辆数，导致里程成本被重复放大。现在改为：  
   `运输成本 = 每公里成本 × 距离 + 每辆搬运成本 × 搬运数量`

7. 问题3原实现默认瞬时调度。  
   现已改为离散时延到站：调度在出发时刻扣减源站库存，在 `t + round(d_ij / v)` 时刻进入目标站库存，其中 `v` 为附件3给出的调度车速。

8. 根目录存在多份伪代码说明和重复总结，且文件扩展名混乱。  
   已统一归档至 `docs/archive/`。

### 仍需在论文 / 报告中说明的限制

1. 问题2样本量只有 7 天，模型精度上限天然受限，结论更适合做课程 / 建模题演示，不适合直接外推到真实运维。

2. 问题3当前采用启发式贪心调度，不是严格数学规划最优解；如果答辩里强调“最优”，需要明确这是“近似优化方案”。

3. 问题3的“满桩惩罚”与“用户还车失败”之间仍存在建模口径差异。当前程序已能稳定对比方案优劣，但若要完全贴合题面，建议进一步把惩罚定义写成严格的数学口径。

---

## 代码说明

### `data_preprocess.py`

负责问题2的数据读取、时间特征、空间特征、前一日滞后特征，以及“仅用训练集构造基线”的数据集划分。

### `model_train_predict.py`

负责 XGBoost 训练、预测与误差评估。

### `main.py`

负责问题2主流程，默认测试日为 `2025-04-13`。

### `problem2_workday_predict.py`

负责生成问题3所需的工作日预测文件，当前目标日为 `2025-04-11`。

### `problem3_config.py`

负责站点数据、参数数据、距离矩阵和预测文件加载。

### `problem3_simulation.py`

负责借还仿真、调度执行、惩罚计算与指标统计。

### `problem3_solution.py`

负责问题3主流程、无调度 / 有调度对比，以及结果落盘。

---

## 建议的后续完善方向

1. 为问题2加入滚动预测或递推预测版本，进一步贴近“未来 24 小时逐时预测”的真实场景。
2. 为问题3补充严格的数学模型表达，包括决策变量、目标函数与约束条件。
3. 补一份 `requirements.txt` 或 `environment.yml`，便于项目复现。
4. 若作为提交材料，建议再单独写一份报告，把“已修正问题”和“模型局限”同步写入正文。
